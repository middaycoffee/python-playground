## ISS Tracker

A tiny Python script that pings the Open-Notify API to extract the real-time geolocation of the International Space Station. 

### Tech Stack
* Python 3
* `requests` library
* `time` module

### How to Run
Clone the repo and run the script in your terminal:
```bash
python track_iss.py